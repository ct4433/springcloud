server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: git@github.com:ct4433/springcloud-config.git
          search-paths:
      label: master


eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
